{
  "name": "aws",
  "version": "1.0.0",
  "description": "POC AWS Lambda padrao Futuro (Terraform + floci)",
  "private": true,
  "scripts": {
    "deploy": "bash scripts/deploy.sh",
    "invoke": "bash scripts/invoke.sh",
    "gen": "plop --plopfile plopfile.js"
  },
  "devDependencies": {
    "plop": "^4.0.1"
  }
}
