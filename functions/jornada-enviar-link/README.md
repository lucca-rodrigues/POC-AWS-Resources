# Function: jornada-enviar-link

Step da jornada: Envia link via Zenvia (mock).

## Comandos Terraform (manual)

```bash
cd terraform/environments/dev-local
terraform init
terraform plan
terraform apply
terraform destroy
```
