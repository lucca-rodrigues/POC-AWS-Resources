# Function: jornada-criar-envelope

Step da jornada: Cria envelope na Click Sign (mock).

## Comandos Terraform (manual)

```bash
cd terraform/environments/dev-local
terraform init
terraform plan
terraform apply
terraform destroy
```
