# Function: jornada-gerar-token

Step da jornada: Gera o invitation token.

## Comandos Terraform (manual)

```bash
cd terraform/environments/dev-local
terraform init
terraform plan
terraform apply
terraform destroy
```
